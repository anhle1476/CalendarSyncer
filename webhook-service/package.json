{
  "name": "calendar-webhook-service",
  "version": "1.0.0",
  "description": "Google Calendar Webhook Service for Calendar Sync",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest",
    "lint": "eslint src/",
    "health-check": "curl -f http://localhost:3000/health || exit 1",
    "local-tunnel": "cloudflared tunnel run my-calendar-tunnel"
  },
  "keywords": [
    "google-calendar",
    "webhook",
    "rabbitmq",
    "calendar-sync"
  ],
  "author": "Calendar Sync Service",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "amqplib": "^0.10.3",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1",
    "joi": "^17.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "eslint": "^8.56.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}