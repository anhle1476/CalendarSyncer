<Window x:Class="CalendarDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalendarDesktop"
        mc:Ignorable="d"
        Title="Calendar Sync Desktop" Height="600" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="10">
            <TextBlock Text="Calendar Sync Desktop" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Calendar Events Section -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Calendar Events" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>
                <Button Name="RefreshButton" 
                        Content="Refresh" 
                        Margin="20,0,0,0" 
                        Padding="10,5" 
                        Click="RefreshButton_Click"/>
            </StackPanel>
            
            <DataGrid Grid.Row="1" 
                      Name="EventsDataGrid" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#F5F5F5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Event ID" Binding="{Binding EventID}" Width="200"/>
                    <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="300"/>
                    <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="End Time" Binding="{Binding EndTime, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <!-- Separator -->
        <GridSplitter Grid.Row="2" 
                      Height="5" 
                      HorizontalAlignment="Stretch" 
                      Background="#E0E0E0"/>
        
        <!-- UDP Messages Section -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="UDP Messages" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>
                <Button Name="ClearMessagesButton" 
                        Content="Clear" 
                        Margin="20,0,0,0" 
                        Padding="10,5" 
                        Click="ClearMessagesButton_Click"/>
            </StackPanel>
            
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto">
                <TextBox Name="UdpMessagesTextBox" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap" 
                         FontFamily="Consolas" 
                         FontSize="12" 
                         Background="#F8F8F8" 
                         BorderThickness="1" 
                         BorderBrush="#CCCCCC" 
                         Padding="5"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
